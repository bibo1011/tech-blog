<article class="post">
    <div class="title">
        <a href="/dashboard/edit/{{id}}">{{title}}</a>
        <span>{{created_at}}</span>
    </div>
    <div class="content">
        <p>
            {{content}} by <span>{{user.username}}</span>
        </p>
    </div>
    
</article>